# ============================================
# J.A. UNIFORMS - Environment Variables
# ============================================
# Copy this file to .env and fill in your values
# cp .env.example .env
# ============================================

# Security
SECRET_KEY=your-secret-key-here-change-in-production
ADMIN_EMAILS=it@jauniforms.com,webadmin@jauniforms.com

# Database
# SQLite (development):
# DATABASE_URL=sqlite:///uniforms.db
# PostgreSQL (production):
DATABASE_URL=postgresql://username:password@localhost:5432/ja_uniforms

# Flask Settings
FLASK_ENV=production
FLASK_DEBUG=False

# Upload Settings
UPLOAD_FOLDER=uploads
MAX_CONTENT_LENGTH=16777216

# Email Configuration (Google Workspace / Gmail)
MAIL_SERVER=smtp.gmail.com
MAIL_PORT=587
MAIL_USE_TLS=True
MAIL_USERNAME=your-email@jauniforms.com
MAIL_PASSWORD=your-app-password-here
MAIL_DEFAULT_SENDER=your-email@jauniforms.com

# ============================================
# NOTES:
# ============================================
# 
# SECRET_KEY:
#   - Generate a secure key: python -c "import secrets; print(secrets.token_hex(32))"
#   - Keep this secret and unique per environment
#
# DATABASE_URL:
#   - Format: postgresql://username:password@host:port/database_name
#
# MAIL_PASSWORD: 
#   - For Gmail/Google Workspace, use an "App Password"
#   - Go to: Google Account → Security → 2-Step Verification → App Passwords
#   - Generate a new app password for "Mail"
#
# ADMIN_EMAILS:
#   - Users with these emails automatically get admin role on registration
#   - Separate multiple emails with commas (no spaces)
#
# MAX_CONTENT_LENGTH:
#   - 16777216 = 16MB max file upload size
#
# ============================================