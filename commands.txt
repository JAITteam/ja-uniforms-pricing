# 1. See which files changed
git status

# 2. Stage all changes
git add .

# 3. Commit with a message
git commit -m "Describe your changes here"

# 4. Push to your remote repository (e.g., GitHub)
git push origin main

.\.venv\Scripts\Activate.ps1    

.\restore_database.ps1

cd "C:\Program Files\Redis"
.\redis-cli.exe
KEYS *

Quick Commands to Remember:
redis-cli pingTest connection
redis-cli info Show Redis info
redis-cli flushall  Clear all data
redis-cli shutdownStop Redis

If Redis Won't Start:
Open Services (Windows + R → services.msc) → Find "Redis" → Right-click → Start